"""
this is all about the menu :)
"""

#class MenuStrategy(object):
    #"""Interface of the menu strategy"""
    #def __init__(self, request):
        #super(MenuStrategy, self).__init__()
        #self.request = request

    #def GenerateMenu(self):
        ## get all the urls for * current path *
        ## filter them on the permission


#class AdminAreaStrategy(MenuStrategy):
    #def GenerateMenu(self):


#def MenuBuilder(request):
    #"""Decide which menu strategy to apply"""
    #if request.path:
    #else:


def get_menu(request):
    #menu_strategy = MenuBuilder(request)
    return {}
